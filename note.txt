ERPNext (Item / Item Group)
        │  REST API, token auth
        ▼
lib/erpnext/client.ts   (ERPNextClient — raw HTTP wrapper)
        │
        ▼
lib/erpnext/sync.ts     (syncCatalogFromERPNext — transform + upsert)
        │
        ▼
Supabase (categories, products, sync_runs tables)
        │
        ▼
lib/erpnext/queries.ts  (typed Supabase reads — getProducts, getCategories, etc.)
        │
        ▼
app/[locale]/products/page.tsx  (Server Components, never touch ERPNext directly)

---------------------------------------------------------------------------------------------------------------------------
    
## CODE QUALITY CHECK CI

    name: CodeQL Analysis

on:
  push:
    branches: [main, development]
    paths:
      - "**/*.ts"
      - "**/*.tsx"
      - "**/*.js"
      - "**/*.mjs"
      - ".github/workflows/codeql.yml"
  pull_request:
    branches: [main, development]
    paths:
      - "**/*.ts"
      - "**/*.tsx"
      - "**/*.js"
      - "**/*.mjs"
      - ".github/workflows/codeql.yml"
  schedule:
    - cron: "0 0 * * 0" # Weekly on Sunday at midnight UTC

jobs:
  analyze:
    name: CodeQL Security Analysis
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write

    strategy:
      fail-fast: false
      matrix:
        language: ["javascript-typescript"]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: ${{ matrix.language }}
          queries: +security-and-quality

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20.x"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Autobuild
        uses: github/codeql-action/autobuild@v3

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v3
        with:
          category: "/language:${{ matrix.language }}"
